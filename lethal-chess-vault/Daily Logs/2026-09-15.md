---
tags: [daily, log]
---

# 2026-09-15 · Kickoff

Daily log. See [[Home]], [[System Map]], [[Decision Log]].

## Summary

Scaffolded **lethal-chess** with `forge`, then reworked the scope and built a playable MVP.

- **Scope narrowed** to a drilling tool for openings. The lethality-visualizer concept from
  `../pre-lethalchess` is parked, not deleted — see
  [[Lineage — from lethality analyzer to drilling tool]].
- **Stack chosen:** [[SvelteKit]] + Svelte 5 runes + TS, [[chess.js]], [[Stockfish]] WASM.
  React was the alternative; rationale and what we gave up are in [[Decision Log]].
- **[[Play vs Computer]] shipped** — full legal chess against the engine, five difficulties,
  drag + click moves, undo, move list.

## Verified, not assumed

- `pnpm check` → 0 errors.
- Engine answers UCI and honours `UCI_Elo` (node harness).
- Browser Worker path proven separately in headless Firefox: worker fetched the WASM, emitted
  `uciok`/`readyok`, returned `bestmove e7e5`. Worth the extra step — node and browser use
  different APIs, and only the browser one is what ships.
- Board render confirmed by screenshot.

## Snags worth remembering

- pnpm 11 ignores the `pnpm` field in `package.json`; settings moved to `pnpm-workspace.yaml`.
  An ignored install script is a *hard* failure that breaks `pnpm check`. See [[pnpm]].
- Current SvelteKit has no `svelte.config.js` — adapter + compiler options live in `vite.config.ts`.
- Stockfish's `UCI_Elo` floor is 1320, so "Beginner" cannot go lower without `Skill Level`.

## Also today (system, not this project)

Bumped the Nix-pinned Claude Code 2.1.220 → 2.1.272 to get `claude-fable-5-1`. Verified the model
ids by grepping the released binary before editing the module. Logged in
`~/myNixOS/system-vault/Decision Log.md`, not here.

## Afternoon — goals widened, audits, catalog

New goals: classify all openings by strength with per-opening weaknesses to drill; drill the English
and Sicilian vs all common responses.

- **Two Fable 5.1 audits** (read-only): [[2026-09-15 — Fable code audit]] and
  [[2026-09-15 — Fable design review]].
- **Code audit → 7 fixes**, the serious one being a stale engine move landing on a new game. Verified
  with a mock of Stockfish's queue semantics (8/8) and again against the real WASM worker.
- **Design review** reshaped the plan: two-number strength model, operational weakness definition,
  offline native pipeline, exploit-first drills, line-walk FSRS. It also caught that the old
  lethality formula never contained engine data — confirmed in code.
- **Verified data:** Lichess eval db (22 GB, CC0) exists; explorer API needs auth; old 2013 cache
  too thin. See [[Data sources]].
- **Catalog imported:** 3,810 lines, all validated through chess.js.

## Next

- Design [[Opening Drills]] — the five open questions in that note, especially scheduling unit and
  failure handling. Worth a second opinion before committing.
- Decide persistence (nothing survives a reload).
- [[Engine licensing]] eventually.
